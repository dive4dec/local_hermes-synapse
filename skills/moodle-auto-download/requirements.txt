# Dependencies for the Moodle Quiz Attempts Downloader tool
# Install with: pip install -r requirements.txt
#
# CDP attach mode: connects to an already-running, already-logged-in browser
# over the Chrome DevTools Protocol. No Selenium, no chromedriver (version
# mismatch-proof), no credentials.

requests>=2.28.0
websocket-client>=1.5.0
